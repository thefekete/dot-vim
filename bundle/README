To install new vim-bundles from github:

    $ git submodule add <github href>

If you forgot to use the --recursive flag for git clone, you can add the
submodules like this:

    $ git submodule init
    $ git submodule update

